<div class="main-wrapper">
  <div class="content">
  	<% @players.each do |player| %>
	  	<div class="elem">
	  		<label>IP:</label><span><%= player.ip %></span>
	  		<label>EMAIL:</label><span><%= player.email %></span>
	  	</div>
  	<% end %>
  </div>
<div>